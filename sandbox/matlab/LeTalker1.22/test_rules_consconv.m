function p = test_rules_consconv()

    InitializeLeTalker

    p = rules_consconv(p, c);

    % L = p.L
    % T = p.t
    % zn = p.zn
    % m = [p.m1, p.m2, p.M]
    % k = [p.k1, p.k2, p.K]
    % kc = p.kc
    % b = [p.b1, p.b2, p.B]

end
